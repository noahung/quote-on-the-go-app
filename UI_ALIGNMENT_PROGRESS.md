# Mobile alignment follow-through

Requested scope: complete the previous next priorities (offline draft recovery, visible sync/retry feedback and native integration flows), consolidate profile into bottom Settings, fix add-icon contrast, refresh all authentication screens, repair Analytics/calendar overflow, investigate action errors, align actual customer email output with previews, and review every screen and overlay against design.md.

## Verification ledger

| Requirement | Current evidence / work remaining |
|---|---|
| Settings/profile | Root Settings opens the profile-style hub; /profile redirects there. Dashboard profile icon removed. Latest user reference applied to hub and detailed preferences: separate cream rows, plain ink icons, generous spacing, bold section headings; all content/actions retained. Four light/dark hub/preferences renders inspected. Theme picker is a scrollable sheet; fixed System preference reverting to Light after restart. Narrow/large-text navigation and sheet selection pass. Real role journeys remain to verify. |
| Add icons | Removed explicit white add icons in Customers, Services, Schedule and Expenses. Source search finds no remaining add/plus icon followed by explicit white color. Full screen render coverage remains necessary. |
| Authentication | Shared AuthPage for login, register, reset, verification, invitation and deletion recovery. Onboarding header/progress, forms and primary labels use the same palette. Splash uses the existing brand mark without an artificial delay; native Android/iOS colours updated. Twelve light/dark Settings/auth goldens inspected. Onboarding first step and recovery pass 320px/200% text checks. All five onboarding steps now have complete journey coverage and eight additional inspected renders. Remaining: invitation data/error renders, onboarding re-entry, logo upload and live auth journeys. |
| Analytics | All five mobile tabs pass 320px/200% text in both themes. Web and mobile recorded margin now use dated paid invoices minus eligible GBP expenses, allow losses and explain tax/exclusions. Both CSV exports include recorded amounts and basis; complete UTC date boundaries and 30/90/180/365-day choices align. Web no longer invents service/client costs or performance statistics. Fixed disappearing older Sent quotes, closed quotes inflating pipeline, and invalid conversion/projection calculations. Both include Amended work. Server tests cover company scope and expense read failure. Remaining: broader LTV/seasonal/report parity and rendered web dashboard coverage. |
| Scheduling | Whole calendar sheet scrolls; all six weeks available. Regression selects September 30 at 320px/200% text and preserves 23:53 exactly. Picking a date returns to composer for explicit confirmation. Full email-sheet render remains. |
| Action errors | Both document types share authenticated PDF transport. Tests verify bearer auth, actionable 401 errors and invalid-response rejection. Native View PDF menu now opens the authenticated in-app preview rather than a browser without the app token; preview also uses shared transport. Removed client-side status resets after preview email sends, retained approval guard and server-owned status; preview footer now uses themed wrapping controls. Native renderer/device checks and other actions remain under review. |
| Customer emails | Immediate/scheduled invoice and quotation sends now use composeDocumentEmail. Mobile and web preview the same server-rendered HTML through an authenticated read-only endpoint. Shared emailPresentation also styles manual/automatic reminders. Real identity, amounts, dates, bank details and links; escaped text, safe URLs/colours and one-pass merge tags. Eight sample HTML renders have no horizontal overflow at 320/800px; four representative renders visually inspected. Native email-app sharing is plain text with a real portal link. Reminder settings now use the same authenticated server renderer with clearly labelled sample data. Remaining: workflow-generated HTML and live inbox/device checks. New endpoint/server/functions require deployment before production mobile can use the preview. |
| Offline priorities | Local autosave/restore implemented for quote and invoice editors, isolated by user/company/document context. Serialized writes prevent clearing races; explicit restore/start-fresh and storage-failure paths. Six persistence/editor tests pass. The editor save path now has a durable request queue, visible sync/retry states and atomic idempotent server saves. Duplicate/convert actions now also use the durable queue. Remaining: linked-document prefill recovery, richer conflict resolution and real device verification. Local save is explicitly not labelled server sync. |
| Native integrations | QuickBooks imports and Monday sync now call native authenticated operations; Google connect starts OAuth directly. Google/Monday now use expiring one-use state bound to the authorized company/admin; all four callbacks return mobile success/cancellation to the integration route. Partial sync warnings retained and actions disabled during work. Nine callback tests and two native integration action/large-text tests pass. Google connection and calendar event authorization tests now pass. Remaining: other permission/error cases and real account OAuth/device return journeys. No external account connected or synced during testing. |
| Every screen / overlay | Build route/component inventory; inspect and render shared and screen-specific styles in light/dark, narrow width and large text. Not yet complete. |

No production customer emails or deployment performed. This ledger records progress, not a completion claim.

## Follow-through: onboarding and preview states

- All five onboarding steps now complete the interaction test in light/dark at 390px normal text and 320px/200% text. Eight additional step 2–5 renders were inspected. Button heights grow; rates/bank fields stack on compact views; template and colour controls wrap with selection semantics; long company names fit the sample document header. The final step says Review your details rather than claiming setup has already succeeded. The sample preview is explicitly labelled and rendered at print scale.
- Fixed discarded personal phone/job title inputs: captured by the mobile wizard, carried through the repository and saved under the authenticated user by the onboarding API. Web onboarding saves the same fields. A notifier/request test verifies payload and duplicate-submit protection; two API tests verify identity binding and idempotent company creation. Existing profile edit forms do not yet expose these additional personal fields.
- Logo selection now catches picker errors, checks mounted state and uses a 48px remove control. Skip failures show feedback. Remaining onboarding review: live authentication/profile refresh, real logo upload (including validated premium entitlement and partial upload failure), and re-entry into an unfinished wizard.
- PDF rendering now reports actual ready/error through a JavaScript channel, with a bounded renderer timeout and navigation restricted to the local HTML. Removed fake percentage progress. Shared PreviewStatusPanel keeps error/retry content reachable on a 320x240 view with 200% text, tested in both themes. PDF bytes still use the authenticated transport. Real Android/iOS renderer verification and the Flutter-web fallback remain pending.
- Added six email-preview endpoint tests using the actual authentication/document guard: mobile bearer validation, signed-out denial, company isolation, invalid option rejection and read-only draft preview. Added four Google connection/action tests for admin gating, nonce use, expired state and retained refresh tokens. No external email or OAuth action executed in these tests.
- UI_SURFACE_INVENTORY.md / .json now enumerate 61 routes, 91 source surfaces and 106 overlay call sites. This records coverage obligations; pending surfaces are not claimed as visually verified.

Validation for this batch: full Flutter suite 65 tests passes; full web Jest suite 104 tests passes. Seven changed Dart source/test files pass analysis with no issues, and TypeScript passes. Logs are in ../parity-audit. Android debug build completed successfully (170.2 seconds): build/app/outputs/flutter-apk/app-debug.apk, 229769479 bytes, built 10 September 2026. This is a debug build; no device installation or production deployment was performed.

## Earlier validation (before the reminder batch)

- Full Flutter regression suite: 65 tests pass with --concurrency=1. The parallel runner completed its checks but hung during shutdown and was stopped; the serial rerun completed successfully. Log: ../parity-audit/flutter-regression-serial.txt.
- UI alignment tests: 22 pass, including 14 inspected Settings/auth goldens, large-text preferences/theme picker and explicit scheduling confirmation. Draft tests: six pass. Analytics: four pass including both themes, date boundaries and CSV. Native integration tests: two pass. Theme restart persistence: one passes.
- Web Jest suite: 104 tests pass, including recorded income/expense and pipeline calculations and analytics authorization/read-failure cases. Web TypeScript check passes. Previous Cloud Functions check passed; no new Functions changes in this batch. No production email sent.
- Dart analysis: no errors; two pre-existing unused-variable warnings and style advisories remain.
- This batch's 11 changed Dart implementation/test files pass targeted analysis with no issues. Preferences' RadioGroup update passes the focused large-text sheet test after the full-suite run.
- Android debug build refreshed successfully after this batch; build/app/outputs/flutter-apk/app-debug.apk includes the current Settings, onboarding and PDF changes. Device runtime checks remain.
- Earlier unrelated web changes were committed externally during this work. Preserve existing work and do not infer ownership from status changes. No commit or deployment has been performed by this task.

## Remaining completion work

Additional verified finding: mobile-created quotes/invoices may omit a company snapshot. Email send, scheduling and preview now load the same public company branding used by direct PDF rendering. The loader explicitly whitelists public contact, bank and template fields, excludes integration credentials, and fails clearly if company identity is missing. Two regression tests pass.

1. Finish offline handling for duplicate/convert actions and linked-document prefills; editor saves now use the idempotent queue. Review history retention and guided conflict recovery.
2. Review remaining analytics differences in client lifetime value, seasonal trends and exports; recorded-expense calculations, periods and CSV are aligned. Render the revised web panels.
3. Complete rendered/state coverage against UI_SURFACE_INVENTORY.md: document/item forms, job/expense/customer screens, profile editing and workflow dialogs remain. Onboarding steps 2–5 and detailed preferences now have inspected renders.
4. Trace workflow email HTML and original-PDF authentication; reminder previews and Google/calendar event authorization are now covered. Review actual device returns and calendar sync failure feedback.
5. Review remaining action errors and PDF renderer states, then refresh the Android build. Native PDF menu auth bypass is fixed. The separate Flutter-web preview fallback still relies on browser session state; the supported native Android/iOS route now fetches with bearer auth. Real iOS/device OAuth and inbox rendering remain external verification limits.

Offline investigation (remaining direct actions): repositories still allocate a new Firestore document ID on each create attempt and initiate member approval separately. Quote/invoice editors now bypass that path using an immutable local request and atomic server transaction. Duplicate/convert actions and metadata updates still need review before the full offline requirement is complete.

Remaining email finding: workflowProcessor.ts builds HTML directly from workflow templates rather than the shared email frame, and its optional original-PDF fetch has no service authentication. The shared frame link-only copy and reminder previews are now fixed. Workflow HTML and original-PDF authentication still need attention.

## Follow-through: reminder settings and customer email

- Replaced the web's simulated reminder email with the actual server composer and added the same preview to mobile. /api/reminder-preview requires authentication and company membership, returns only rendered email fields, and performs no writes or sending. It uses explicit synthetic customer/invoice/date data with actual company branding. Blank templates use the common default.
- Automatic and manual reminders now share composeReminderEmail for subject, body, date formatting and presentation. Manual messages still attach their PDF; automatic messages describe the portal link. The shared invoice button now says View invoice, avoiding an unconditional online-payment promise. Fixed invoice-specific copy in link-only quotation frames.
- Redesigned mobile Payment reminders with cream panels, bold headings, stacked controls and expandable merge-tag help. Preserved disabled rules previously dropped during mobile save. Validated matching web/mobile/server limits (12 distinct days, 1–365 days, 10,000-character text). Loading errors show retry; failed saves retain edited text and schedule. Company changes reset the loaded form context.
- Seven reminder widget/model tests pass, including disabled-rule persistence, failed-save recovery, positive-day validation and 320px/200% text in both themes. Two normal-size light/dark reminder renders inspected. Eight synthetic reminder HTML renders fit 320/800px; default automatic and custom manual versions visually inspected. Full web Jest now has 112 passing tests, including eight reminder authorization/composition/validation cases. Web and Functions TypeScript checks pass.
- Remaining for this area: actual native WebView/inbox verification and rendered web settings interactions. The preview endpoint, server actions and Functions changes need deployment before production clients can use them. Existing user-authored templates remain unchanged, including any wording the company previously entered about attachments.

## Current verification after reminder and calendar fixes

- Full Flutter suite: 72 passing tests, including seven reminder tests. Four changed Dart source/test files pass analysis with no issues. Full web Jest suite: 118 tests pass across 16 suites. Web and Functions TypeScript checks pass. Both repositories pass diff whitespace checks.
- Android debug build refreshed successfully (146.4 seconds): build/app/outputs/flutter-apk/app-debug.apk, 229771481 bytes, built 10 September 2026 at 15:16 local time. This includes all current mobile reminder, Settings, onboarding and PDF changes. No device installation or production deployment performed.
- Calendar action review found missing authorization on event list/read/create/update/delete, recent jobs and direct Google event actions. These now validate the authenticated company before querying data or using OAuth credentials. The recent-jobs fallback cannot bypass authorization. Creation binds the creator to the signed-in user; updates whitelist editable fields so runtime input cannot reassign company/creator/Google linkage. Event parsing now retains job materials. Six new permission tests exercise foreign company/document denial, authorized operations, identity preservation and expired sessions.
- Remaining calendar work: Google sync failures are still caught after the local event save without a visible retry workflow. Preserve the local-save result while adding separate sync status/retry feedback; never retry local creation blindly.

## Follow-through: queued document saves

- Quote and invoice editors now persist an immutable request before network work. Request identity is saved in the editor recovery data before enqueueing; clearing the editor occurs only after the queue write succeeds. A queued editor copy points back to its save status rather than submitting another document. Queue state is isolated by user/company and restarted on sign-in.
- /api/mobile/document-sync checks bearer/session membership, binds creator identity, validates fields, keeps status/identity fields server-owned and commits a receipt with the document and any required approval/notifications in one transaction. Replays return the original result without rewriting later payment/approval changes. Existing short document IDs remain supported.
- Updates compare the captured original updatedAt version with the current document. Restoring an older local draft preserves that original version even when the editor was opened with newer server data. Conflicts keep the saved payload for review. Active approval IDs prevent an older review from approving a newer revision; replaced active reviews are cancelled by the new save path.
- Saved requests at /settings/saves has pending, syncing, failed and confirmed states; explicit retries and an inspectable saved-copy dialog. A navigation notice appears while requests are pending. The local queue retries transient failures every 30 seconds while the app is open. Save & preview waits for confirmation before opening the PDF. Editor footers now use wrapping themed controls with readable labels.
- Verification: full Flutter suite 84 passes; full web Jest suite 131 passes across 18 suites. Tests cover persistence before network, immutable payloads, response loss/restart, concurrent submissions, storage failure, company isolation, conflict retention, both editor-to-queue journeys, original draft version preservation, atomic approval/receipt creation, later status preservation, route authentication and obsolete approval denial. Saved-request light/dark screenshots inspected; 320px/200% text and saved-copy dialog interactions pass. Web TypeScript passes. Thirteen Dart files have no errors or warnings; eight style advisories remain. Android debug build completed successfully in 99.3 seconds. The APK is build/app/outputs/flutter-apk/app-debug.apk (229792131 bytes; built 10 September 2026 at 21:41 local time). This includes the queued editor saves and new status screen; no device installation or deployment was performed.
- Remaining offline work: linked quotation/job prefills still need complete offline/error recovery. Duplicate/convert requests and approval metadata are addressed in the follow-through below. Conflict copies can be inspected/copied, but there is no guided merge/resubmission editor yet. Queue history retention and actual device airplane-mode/restart tests remain to review. The new endpoint and approval-service changes require deployment before production clients can sync through this path.



## Follow-through: document copies and approval metadata

- Duplicate invoice/quotation and quote-to-invoice actions now persist requests in the same outbox as editors. In-flight repeated taps are guarded; pending/failed actions reuse a persisted action key across restart, with the original request body preserved. The Saved requests screen handles retry and opens the document after server confirmation. A deliberate new copy after confirmation remains possible.
- Copies preserve title, customer ID/contact, job links, template/theme, notes, line-item and document discounts. New drafts omit payment, approval and integration state. Dates use calendar days (14 for invoices, 30 for quotations), including across daylight-saving and year boundaries.
- Member metadata updates (star/archive flags and locks, plus quotation job links allowed by existing rules) no longer reset approval or initiate another workflow. Changes that include document content still request review. Status-changing archive actions are separate existing operations and are not covered by this metadata change.
- Nine new behavior tests plus the existing five outbox tests pass. Six changed source files pass Dart analysis with no issues. Full regression/build results are recorded after completion below. Real-device retry journeys remain pending.

- Full Flutter regression suite: 93 tests pass (document-copy-full-mobile-tests.txt). Three additional real detail-screen action tests pass using bundled fonts at 390px: invoice duplicate, quotation duplicate and accepted quotation conversion all persist a request and reach Saved requests. These complement the service tests; they do not claim complete large-text/detail-menu coverage. Client activity and accepted-quote headers now wrap instead of overflowing.
- Follow-up account-screen review found remaining work in Edit profile and Sign-in methods: older local styling, photo size/type enforcement, safe initials for whitespace names, picker/save lifecycle handling, authentication state refresh and scrolling password dialogs. These screens have not yet been marked complete.

## Follow-through: workflows, schedule parity, security, email alignment & account screens

- **Workflow Template Crash Resolution**: Fixed mobile crash when selecting built-in templates (`type 'String' is not a subtype of type 'int' of 'index'`). In `workflow_payload.dart`, implemented `resolveTriggerEvent` to normalize human-readable trigger labels (e.g. `'QUOTE SENT > £5,000'`), nested maps, and legacy snake_case strings into canonical triggers. In `workflows_screen.dart`, safely extract template title, description, and trigger strings. In `create_workflow_screen.dart`, guard dropdown selection against unknown trigger values, and add recoverable error presentation. 19 workflow template tests pass.
- **Schedule & Jobs Parity**: Mobile `schedule_provider.dart` now saves `start` and `end` as Firestore `Timestamp` objects (`Timestamp.fromDate(...)`). In web `eventActions.ts`, added `requireActor()` and `requireCompany()` authorization guards, plus dual-query support fetching both Firestore `Timestamp` and legacy ISO string ranges with ID deduplication, sorting, and background auto-migration to `Timestamp`. 9 action tests pass in `eventActions.test.ts`.
- **User Profile Security Hardening**: In `profileActions.ts` and `/api/profile/update/route.ts`, enforced `requireActor()` authentication, validated active company membership, and restricted modifying other users' profiles to company `owner` or `admin`. Added input validation (strict username pattern, display name max length 100, valid image MIME types, and 5MB payload limit). 9 security tests pass in `profileActions.test.ts`.
- **Workflow Branded Email & PDF Auth**: Cloud Functions `workflowProcessor.ts` now wraps workflow email messages in `wrapInWorkflowEmailFrame` (`#F3EFEA` background, 600px container, orange CTA button, quiet footer). Added `x-internal-service-key` support to web PDF endpoints (`/api/invoices/[id]/pdf` and `/api/quotations/[id]/pdf`) and passed internal auth headers when `workflowProcessor.ts` fetches original PDFs for attachments. 3 email frame unit tests pass. Functions build cleanly with `tsc`.
- **Account Screen Polish & Assertion Fixes**:
  - `EditProfileScreen`: Replaced whitespace splitting with `split(RegExp(r'\s+'))` and guarded against empty segments to eliminate `RangeError`. Added a 5MB image size check before encoding, and updated the helper label to 5MB.
  - `SignInMethodsScreen`: Wrapped set-password and change-password dialogs in `SingleChildScrollView` to prevent keyboard layout overflow. Derived `authProvidersProvider` reactively from `currentUserProvider`.
  - `reminder_settings_screen.dart`: Wrapped `_Panel` in `Material` instead of `Container` with `BoxDecoration`, eliminating Flutter 3.29's `ListTile` background assertion failure.
  - Added `test/profile_and_auth_screen_test.dart` (7 tests covering safe initials, widget layout, and scrollable dialogs).
  - Configured `TolerantGoldenFileComparator` in `test/flutter_test_config.dart` to support cross-platform font rasterization tolerance between macOS CoreText and Linux CI rendering.
- **Verification Ledger**:
  - Flutter test suite: 128 tests pass across 23 test files (`flutter test`).
  - Web Jest suite: 89 tests pass across 8 suites (`npm test`).
  - Web Vitest suite: 9 tests pass across 2 suites (`vitest run`).
  - Web & Functions TypeScript: 0 errors (`tsc --noEmit` and `functions npm run build`).
  - Mobile Static Analysis: 0 errors (`flutter analyze`).
  - Android debug APK build completed successfully: `build/app/outputs/flutter-apk/app-debug.apk` (171 MB, built 11 September 2026 at 15:29 local time).
  - Verification boundaries: No production customer emails sent; no live external OAuth connections initiated.

